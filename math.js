const num1 = 12;
const num2 = 13;
const total = 0

total = num1 + num2;
//trancating
let trunc = Math.trancate(total);
let final  = (Math.sqrt(total));
//gettting the power of final
let power = Math.pow(final, 2);
